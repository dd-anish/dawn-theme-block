<div class="button-div-class">
  <button
    data-theme-block-id="{{ block.id }}"
    class="theme-block-button {{ block.settings.button_type }} {% if block.settings.button_url == blank %} no-url {% endif %} {% if block.settings.disable_hover %} no-hover {% endif %}"
    type="button"
    href="{{ block.settings.button_url }}">
    {{ block.settings.button_text }}
  </button>
</div>

{% schema %}
  {
    "name": "Button",
    "settings": [
      {
        "type": "text",
        "id": "button_text",
        "label": "Button Label",
        "default": "Button"
      },
      {
        "type": "text",
        "id": "button_url",
        "label": "Button URL"
      },
      {
        "type": "select",
        "id": "button_type",
        "label": "Button Type",
        "options": [
          {
            "value": "primary",
            "label": "Primary"
          },
          {
            "value": "secondary",
            "label": "Secondary"
          },
          {
            "value": "tertiary",
            "label": "Tertiary"
          }
        ],
        "default": "primary"
      },
      {
        "type": "checkbox",
        "id": "disable_hover",
        "label": "Disable Hover Effect",
        "default": false
      }
    ],
    "presets": [
      {
        "name": "Button"
      }
    ]
  }
{% endschema %}